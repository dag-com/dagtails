name: Legal watch

on:
  push:
    branches: [master]
  pull_request:

permissions:
  contents: read

jobs:
  legal-watch:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Scan new additions
        env:
          LEGAL_WATCH_BASE: ${{ github.event_name == 'pull_request' && format('origin/{0}', github.base_ref) || github.event.before }}
        run: python .cursor/skills/legal-watch/scripts/scan.py --outgoing --gate
