# Path A — Expo remote preview

This branch adds `mobile/`: an Expo Go WebView shell around the existing web game.

Full instructions: [mobile/README.md](mobile/README.md)

```bash
cd mobile
npm run tunnel
```

Scan the QR with Expo Go (App Store build = SDK 54; this shell matches that). Default content URL is GitHub Pages.
