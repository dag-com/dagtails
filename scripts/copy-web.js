// Legacy helper kept for docs / muscle memory.
// The web bundle is produced by Vite (`npm run build` → `www/`), which also
// copies `assets/` into the output. Capacitor reads `webDir: "www"`.
console.log("Use `npm run build` (Vite). This script is a no-op.");
